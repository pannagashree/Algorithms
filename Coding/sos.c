#include <stdio.h>
#include<string.h>
int main()
{
    /*char str[100],sub[3];
    int j=0,count=0;
    scanf("%s",str);
    int len=strlen(str);
    int np=len/3;
    for(int i=0;i<np;i++)
    {
        sub[0]=str[j];
        sub[1]=str[j+1];
        sub[2]=str[j+2];
        j=j+3;
        if(sub[0]!='S')
            count++;
        if(sub[1]!='O')
            count++;
        if(sub[2]!='S')
            count++;
    }
    printf("%d",count);
    return 0;*/
        int i;
    i = scanf("%d %d", &i, &i);
    printf("%d\n", i);
    return 0;
}

